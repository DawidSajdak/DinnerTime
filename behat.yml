default:
    suites:
        guest:
            contexts:
                - RestaurantContext
                - MenuCardContext
            paths:
                features: %paths.base%/features/Guest